# CS 686 project
# Minimal setup

Make sure the problemset folder is in the same directory as the .py file. \
Specify the folder name in the .py file's main section. \
Run


## Problemsets
The problems are originally for weighted maxsat, so the clauses include weights and solutions for the weighter instances are in the weights_solutions folder.